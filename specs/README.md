# specs/ — contrats de module (Spec-Driven léger)

Chaque **nouveau cours** ou **rewrite majeur** ajoute ou met à jour un fichier `specs/<module-id>.md` avant (ou dans la même PR que) le HTML.

Voir aussi `AGENTS.md` (gates BMAD / Spec Kit) et `specs/_template-module.md`.

## Contrats présents

| ID | Titre | Statut |
|---|---|---|
| `m1p1-refresher-neural-nets` | Refresher Neural Nets (hors brochure) | implemented |
| `toc-double-numbering-fix` | Fix double numérotation TOC cours FR+EN | implemented |
